export const metadata = {
  title: "Create New Test | Evalio Admin - Test Builder",
  description: "Create a new online exam on Evalio - configure test settings, assign questions, set time limits, and customize exam rules.",
};

export default function AddTestLayout({ children }) {
  return children;
}
