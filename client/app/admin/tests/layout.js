export const metadata = {
  title: "Manage Tests | Evalio Admin - Test Administration",
  description: "Admin panel to create, manage, and configure online exams - set test parameters, assign subjects, and control test availability.",
};

export default function TestsLayout({ children }) {
  return children;
}
