export const metadata = {
  title: "Forgot Password | Evalio - Reset Your Account",
  description: "Reset your Evalio account password securely. Enter your email to receive password recovery instructions.",
};

export default function ForgetPasswordLayout({ children }) {
  return children;
}
